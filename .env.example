# Environment variables for MusicList for Soundiiz
# Copy this file to .env and adjust values for your local setup.

# Enable verbose bootstrap logs (only affects log verbosity).
BOOTSTRAP_DEBUG="false"

# Optional default output directory used by local workflows/docs.
OUTPUT_DIR="./output"
